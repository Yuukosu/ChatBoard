<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title><%= @title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>
  <body style="background-color: #E4E4E4;">
    <div class="container-fluid">
      <header class="header fixed-top" style="background-color: ghostwhite;">
        <nav class="navbar navbar-light bg-light">
          <a class="navbar-brand fs-1 fw-bold" href="/" style="margin-left: 30px;">
            <%= @header_title %>
          </a>
          <ul class="list-group list-group-horizontal" style="margin-right: 30px;">
            <li class="list-group-item" style="border: none; background-color: ghostwhite;">
              <form action="/api/search" method="post">
                <div class="form-group d-flex">
                  <label for="search-box"></label>
                  <input class="form-control" type="form-control" id="search-box" placeholder="スレッド検索" size="16" required>
                  <button class="btn btn-outline-dark text-nowrap" type="submit">
                    <img src="/icons/search.svg" alt="search-icon">
                    検索
                  </button>
                </div>
              </form>
            </li>
            <li class="list-group-item" style="border: none; background-color: ghostwhite;">
              <a class="nav-link text-decoration-none" href="#">ログイン</a>
            </li>
          </ul>
        </nav>
      </header>
      <main style="padding-top: 100px; padding-bottom: 100px;">
        <div class="row d-flex d-column">
          <% if @threads.length > 0 %>
            <ul class="col-9 d-grid gap-1" style="list-style: none;">
              <% @threads.reverse_each do |thread| %>
                <li>
                  <a class="link-dark" style="text-decoration: none;" href="#">
                    <div class="p-3 bg-light border" style="background-color: white;">
                      <span class="text-nowrap overflow-hidden" style="color: navy;">
                        <%= (thread.title.length > 32 ? thread.title[0, 32] + "..." : thread.title) + " (#{thread.messages.length})" %><br>
                      </span>
                      <span style="color: green; margin-right: 10px;">
                        <%= ChatBoard::Board.get(thread.board) %>
                      </span>
                      <span style="color: gray;">
                        <%= thread.getStrTime %>
                      </span>
                    </div>
                  </a>
                </li>
              <% end %>
            </ul>
          <% else %>
            <div class="col">
              <p class="text-center">現在この板のスレッドはありません。</p>
            </div>
          <% end %>
          <ul class="col-2" style="list-style: none; margin-right: 30px; margin-left: 0px; background-color: white;">
            <% @boards.reverse_each do |board| %>
              <li>
                <a class="link-primary" href="/index?board=<%= board %>"><%= ChatBoard::Board.get(board) %></a>
              </li>
            <% end %>
          </ul>
        </div>
      </main>
      <footer class="fixed-bottom" style="padding: 16px; background-color: ghostwhite;">
        <article class="text-center">
          © Yuukosu<br>
          転載禁止
        </article>
      </footer>
    </div>
  </body>
</html>
